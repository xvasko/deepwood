<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="row">
    <%= render @users %> <!-- Rails automatically looks for a partial called _user.html.erb -->
</div>

<%= will_paginate :previous_label => '&laquo;', :next_label => '&raquo;' %>